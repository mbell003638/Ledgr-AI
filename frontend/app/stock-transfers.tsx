import React, { useCallback, useMemo, useState } from "react";
import { View, Text, StyleSheet, Pressable, ScrollView, TextInput, Alert, ActivityIndicator } from "react-native";
import { useFocusEffect, useRouter } from "expo-router";
import { SafeAreaView } from "react-native-safe-area-context";
import { Ionicons } from "@expo/vector-icons";
import { useTheme } from "@/src/context/ThemeContext";
import { api } from "@/src/api";
import { isCapabilityEnabled } from "@/src/utils/capabilities";

export default function StockTransfersScreen() {
  const theme = useTheme(); const styles = useMemo(() => makeStyles(theme), [theme]); const router = useRouter();
  const [settings, setSettings] = useState<any>({}); const [locations, setLocations] = useState<any[]>([]); const [transfers, setTransfers] = useState<any[]>([]); const [fromId, setFromId] = useState(""); const [toId, setToId] = useState(""); const [sku, setSku] = useState(""); const [quantity, setQuantity] = useState(""); const [saving, setSaving] = useState(false); const [loading, setLoading] = useState(true);
  const load = useCallback(async () => { try { const [s, l, t] = await Promise.all([api.getSettings(), api.listLocations(), api.listStockTransfers()]); setSettings(s); setLocations(l.filter((item: any) => item.active !== false)); setTransfers(t); if (!fromId && l[0]) setFromId(l[0].id); if (!toId && l[1]) setToId(l[1].id); } catch (error: any) { Alert.alert("Could not load transfers", error?.message || "Try again."); } finally { setLoading(false); } }, [fromId, toId]);
  useFocusEffect(useCallback(() => { load(); }, [load]));
  const create = async () => { const qty = Number(quantity); if (!fromId || !toId || fromId === toId) return Alert.alert("Choose two locations", "The source and destination must be different."); if (!sku.trim() || !Number.isFinite(qty) || qty <= 0) return Alert.alert("Check transfer details", "Enter a product/SKU and a positive quantity."); setSaving(true); try { const transfer = await api.createStockTransfer({ fromLocationId: fromId, toLocationId: toId, sku: sku.trim(), quantity: qty, status: "in_transit", date: new Date().toISOString().slice(0, 10) }); setTransfers((current) => [transfer, ...current]); setSku(""); setQuantity(""); } catch (error: any) { Alert.alert("Could not create transfer", error?.message || "Try again."); } finally { setSaving(false); } };
  if (!loading && !isCapabilityEnabled(settings, "multi_location")) return <SafeAreaView style={styles.container}><View style={styles.center}><Ionicons name="swap-horizontal-outline" size={40} color={theme.color.brandPrimary} /><Text style={styles.title}>Stock transfers are off</Text><Text style={styles.sub}>Enable Multi-location retail from Settings to move stock between locations.</Text><Pressable onPress={() => router.replace("/customize-features")} style={styles.primary}><Text style={styles.primaryText}>Open capabilities</Text></Pressable></View></SafeAreaView>;
  return <SafeAreaView style={styles.container} edges={["top"]}><View style={styles.header}><Pressable onPress={() => router.back()} style={styles.back}><Ionicons name="arrow-back" size={23} color={theme.color.onSurface} /></Pressable><View style={{ flex: 1 }}><Text style={styles.headerTitle}>Stock transfers</Text><Text style={styles.headerSub}>Move stock with a clear source and destination.</Text></View></View><ScrollView contentContainerStyle={styles.content}><View style={styles.formCard}><Text style={styles.sectionTitle}>Create transfer</Text><TextInput value={sku} onChangeText={setSku} placeholder="Product or SKU" placeholderTextColor={theme.color.muted} style={styles.input} /><TextInput value={quantity} onChangeText={setQuantity} placeholder="Quantity" placeholderTextColor={theme.color.muted} keyboardType="decimal-pad" style={styles.input} /><Text style={styles.label}>From</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: 8, marginBottom: 10 }}>{locations.map((location) => <Pressable key={location.id} onPress={() => setFromId(location.id)} style={[styles.chip, fromId === location.id && styles.selected]}><Text style={[styles.chipText, fromId === location.id && { color: theme.color.brandPrimary }]}>{location.name}</Text></Pressable>)}</ScrollView><Text style={styles.label}>To</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ gap: 8, marginBottom: 12 }}>{locations.map((location) => <Pressable key={location.id} onPress={() => setToId(location.id)} style={[styles.chip, toId === location.id && styles.selected]}><Text style={[styles.chipText, toId === location.id && { color: theme.color.brandPrimary }]}>{location.name}</Text></Pressable>)}</ScrollView><Pressable onPress={create} disabled={saving} style={[styles.primary, saving && { opacity: 0.5 }]}>{saving ? <ActivityIndicator color="#fff" /> : <Text style={styles.primaryText}>Create transfer</Text>}</Pressable></View><Text style={styles.sectionTitle}>Recent transfers</Text>{transfers.length === 0 ? <View style={styles.empty}><Ionicons name="cube-outline" size={28} color={theme.color.muted} /><Text style={styles.emptyTitle}>No transfers yet</Text><Text style={styles.sub}>Transfers preserve a traceable stock trail between locations.</Text></View> : transfers.map((transfer) => { const from = locations.find((item) => item.id === transfer.fromLocationId)?.name || "Unknown"; const to = locations.find((item) => item.id === transfer.toLocationId)?.name || "Unknown"; return <View key={transfer.id} style={styles.transferCard}><View style={styles.transferIcon}><Ionicons name="swap-horizontal-outline" size={21} color={theme.color.brandPrimary} /></View><View style={{ flex: 1 }}><Text style={styles.transferTitle}>{transfer.sku} · {Number(transfer.quantity || 0)}</Text><Text style={styles.transferSub}>{from} → {to} · {transfer.status || "in_transit"}</Text></View><Text style={styles.date}>{transfer.date || ""}</Text></View>; })}</ScrollView></SafeAreaView>;
}
function makeStyles(theme: any) { return StyleSheet.create({ container: { flex: 1, backgroundColor: theme.color.surface }, center: { flex: 1, alignItems: "center", justifyContent: "center", padding: 28 }, title: { color: theme.color.onSurface, fontSize: 20, fontWeight: "800", marginTop: 14, textAlign: "center" }, sub: { color: theme.color.muted, fontSize: 13, lineHeight: 19, textAlign: "center", marginTop: 7 }, header: { flexDirection: "row", alignItems: "center", gap: 10, padding: 16, borderBottomWidth: 1, borderBottomColor: theme.color.border }, back: { padding: 4 }, headerTitle: { color: theme.color.onSurface, fontSize: 18, fontWeight: "800" }, headerSub: { color: theme.color.muted, fontSize: 11, marginTop: 2 }, content: { padding: 16, paddingBottom: 40 }, formCard: { padding: 15, borderRadius: 18, backgroundColor: theme.color.surfaceSecondary, borderWidth: 1, borderColor: theme.color.border, marginBottom: 22 }, sectionTitle: { color: theme.color.onSurface, fontSize: 15, fontWeight: "800", marginBottom: 10 }, label: { color: theme.color.muted, fontSize: 11, fontWeight: "800", marginBottom: 6 }, input: { borderWidth: 1, borderColor: theme.color.border, borderRadius: 13, padding: 12, color: theme.color.onSurface, backgroundColor: theme.color.surface, marginBottom: 9 }, chip: { paddingVertical: 9, paddingHorizontal: 11, borderRadius: 14, borderWidth: 1, borderColor: theme.color.border }, selected: { borderColor: theme.color.brandPrimary, backgroundColor: theme.color.brandPrimary + "12" }, chipText: { color: theme.color.muted, fontSize: 12, fontWeight: "700" }, primary: { backgroundColor: theme.color.brandPrimary, padding: 13, borderRadius: 13, alignItems: "center" }, primaryText: { color: "#fff", fontWeight: "800" }, empty: { alignItems: "center", padding: 28, borderWidth: 1, borderColor: theme.color.border, borderRadius: 18, borderStyle: "dashed" }, emptyTitle: { color: theme.color.onSurface, fontWeight: "800", marginTop: 8 }, transferCard: { flexDirection: "row", alignItems: "center", gap: 10, padding: 14, borderRadius: 16, backgroundColor: theme.color.surfaceSecondary, borderWidth: 1, borderColor: theme.color.border, marginBottom: 9 }, transferIcon: { width: 40, height: 40, borderRadius: 20, justifyContent: "center", alignItems: "center", backgroundColor: theme.color.brandPrimary + "14" }, transferTitle: { color: theme.color.onSurface, fontWeight: "800", fontSize: 13 }, transferSub: { color: theme.color.muted, fontSize: 11, marginTop: 3 }, date: { color: theme.color.muted, fontSize: 10 } }); }
